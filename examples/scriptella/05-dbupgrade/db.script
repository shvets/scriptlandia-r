CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE ACCOUNT(ACCOUNT_ID INTEGER NOT NULL PRIMARY KEY,LOGIN VARCHAR(20),PASSWORD VARCHAR(20),SUSPENDED BOOLEAN DEFAULT false NOT NULL)
CREATE MEMORY TABLE ACCOUNT_DETAILS(ACCOUNT_ID INTEGER,EMAIL VARCHAR(20),CONSTRAINT SYS_FK_47 FOREIGN KEY(ACCOUNT_ID) REFERENCES ACCOUNT(ACCOUNT_ID))
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 20
SET SCHEMA PUBLIC
INSERT INTO ACCOUNT VALUES(1,'user','password',FALSE)
INSERT INTO ACCOUNT_DETAILS VALUES(1,NULL)
